<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SqlPad.Oracle.ToolTips"
    xmlns:sqlPad="clr-namespace:SqlPad;assembly=SqlPad">

	<Style TargetType="{x:Type local:PopupBase}">
		<Style.Resources>
			<sqlPad:ObjectToVisibilityConverter x:Key="ObjectToVisibilityConverter" />
		</Style.Resources>

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:PopupBase}">
					<Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
						<StackPanel>
							<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Visibility="{TemplateBinding IsPinnable, Converter={StaticResource ObjectToVisibilityConverter}}">
								<Button x:Name="ButtonPin" Content="Pin" Command="{x:Static local:PopupBase.PinPopupCommand}" />
							</StackPanel>

							<ContentPresenter />

						</StackPanel>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
