<UserControl x:Class="SqlPad.Oracle.ToolTips.ToolTipView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:toolTips="clr-namespace:SqlPad.Oracle.ToolTips"
             mc:Ignorable="d" 
             d:DesignHeight="98" d:DesignWidth="570"
			 Padding="4" d:DataContext="{d:DesignInstance toolTips:ViewDetailsModel}">
    <StackPanel>
		<GroupBox BorderThickness="0">
			<GroupBox.Header>
				<TextBlock Text="{Binding Title}" FontWeight="Bold"/>
			</GroupBox.Header>
			<TextBlock Text="{Binding Comment}" Visibility="{Binding CommentVisibility}" />
		</GroupBox>
		
		<toolTips:ConstraintList Padding="0" Constraints="{Binding ConstraintDetails}" Visibility="{Binding ConstraintDetailsVisibility}" />
	</StackPanel>
</UserControl>
